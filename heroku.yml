# heroku.yml

build:
  docker:
    # This tells Heroku to build the Docker image using the
    # Dockerfile located in the root directory.
    web: Dockerfile

run:
  # This is the command to start the QuickChart server.
  # It automatically uses the $PORT provided by Heroku.
  web: node index.js
