# heroku.yml

build:
  docker:
    web: Dockerfile

release:
  # This command runs *after* the build but *before* the app starts
  # It sets up or migrates the PostgreSQL database
  command:
    - yarn prisma:deploy
  image: web

run:
  # This is the command to start the Linkwarden server
  web: yarn start
